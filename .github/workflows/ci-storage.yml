name: ci-storage

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "jump_storage/**"
      - ".github/workflows/ci-storage.yml"
  push:
    branches: [main]
    paths:
      - "jump_storage/**"
      - ".github/workflows/ci-storage.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jump_storage

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (ruff)
        run: ruff check jump_storage tests

      - name: Tests (pytest)
        run: pytest -ra
